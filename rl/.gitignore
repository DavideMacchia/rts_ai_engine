# Training run outputs (tensorboard, session_log.json, checkpoints, final_model.zip).
# These are regenerable and large (a single 500k run is ~5 MB; older runs were 44 MB/model).
logs_*/
curriculum_logs/
ppo_model/

# Trained model checkpoints are regenerable outputs (`pretrain_bc` produces one in
# minutes); they are not committed. Reproduce from code, then evaluate what you trained.
checkpoints/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
